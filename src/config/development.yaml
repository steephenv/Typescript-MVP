# configuration for cubet pcs
# every loacal pc's should own a mongodb as its own
app:
  logging: true
  morgan: true
  port: 7000

docsUrl: 'localhost:7000'

database:
  url: mongodb://steephen:A!2345@ds237363.mlab.com:37363/htmlnode
