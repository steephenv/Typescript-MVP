
/project/share-project:
  post:
    security:
    - Bearer: []
    summary: api for share project.
    tags:
    - share-project
    description: api for share project.
    parameters:
    - name: X-Requested-With
      description: set value to XMLHttpRequest if you are making an xhr request
      in: header
      required: true
      default: XMLHttpRequest
    # - name: userId
    #   description: 'userId'
    #   in: query
    #   type: string
    - name: Body
      in: body
      required: true
      schema:
        type: object
        required:
        - type
        - sharedLink
        - sharedTo
        properties:
          type:
            type: string
          sharedLink:
            type: string
          sharedTo:
            type: string
    responses:
      200:
        description: "successful attempt, returns `success message`"



