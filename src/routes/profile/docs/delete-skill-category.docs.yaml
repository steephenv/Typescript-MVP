/profile/delete-skill-category:
  post:
    security:
    - Bearer: []
    summary: api for soft delete skill category and sub category.
    tags:
    - profile
    description: api for soft delete skill category and sub category.
    parameters:
    - name: X-Requested-With
      description: set value to XMLHttpRequest if you are making an xhr request
      in: header
      required: true
      default: XMLHttpRequest
    - name: Body
      in: body
      required: true
      schema:
        type: object
        required:
        - model
        - _id
        properties:
          model:
            type: string
            description: name whether it is category or sub category
            example: category , subCategory
          _id:
            type: string
            description: _id of collection     
    responses:
      200:
        description: "successful attempt, returns `success message`"