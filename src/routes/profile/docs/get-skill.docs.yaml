/profile/get-skill-category:
  get:
    security:
    - Bearer: []
    summary: api for get skill details.
    tags:
    - profile updation
    description: api for getting skill details.
    parameters:
    - name: X-Requested-With
      description: set value to XMLHttpRequest if you are making an xhr request
      in: header
      required: true
      default: XMLHttpRequest
    responses:
      200:
        description: "successful attempt, returns `success message`"