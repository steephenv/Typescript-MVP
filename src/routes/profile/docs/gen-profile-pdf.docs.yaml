/profile/gen-pdf:
  get:
    security:
    - Bearer: []
    summary: api for pdf generation of profile.
    tags:
    - profile updation
    description: api for pdf generation of profile.
    parameters:
    - name: X-Requested-With
      description: set value to XMLHttpRequest if you are making an xhr request
      in: header
      required: true
      default: XMLHttpRequest
    - name: userId
      in: query
      required: true
    responses:
      200:
        description: "successful attempt, returns `success message`"