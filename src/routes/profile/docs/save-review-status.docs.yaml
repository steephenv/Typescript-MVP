/profile/save-review-status:
  get:
    security:
    - Bearer: []
    summary: api for save review status.
    tags:
    - profile updation
    description: api for save review status.
    parameters:
    - name: X-Requested-With
      description: set value to XMLHttpRequest if you are making an xhr request
      in: header
      required: true
      default: XMLHttpRequest 
    responses:
      200:
        description: "successful attempt, returns `success message`"