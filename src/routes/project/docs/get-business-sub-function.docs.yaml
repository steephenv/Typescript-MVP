/project/get-business-sub-category:
  get:
    security:
    - Bearer: []
    summary: api for get business sub category details.
    tags:
    - projects
    description: api for get business sub category details.
    parameters:
    - name: X-Requested-With
      description: set value to XMLHttpRequest if you are making an xhr request
      in: header
      required: true
      default: XMLHttpRequest
    - name: subCategoryId
      in: query
      required: true  
    responses:
      200:
        description: "successful attempt, returns `success message`"