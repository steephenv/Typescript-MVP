/projects/save-key-param:
  post:
    security:
    - Bearer: []
    summary: api for save project key parameters details.
    tags:
    - project request
    description: api for save project key parameters details.
    parameters:
    - name: X-Requested-With
      description: set value to XMLHttpRequest if you are making an xhr request
      in: header
      required: true
      default: XMLHttpRequest
    - name: userId
      description: 'userId'
      in: query
      type: string
    - name: Body
      in: body
      required: true
      schema:
        type: object
        required:
        - currentStatus
        - currentSituation
        - challengeType
        - challenge
        - degreeOfChallenge
        - goalValueAdd
        - desiredFutureSituation
        - targetStart
        - expectedEnd
        - mainLocation
        - additionalLocations
        - location2
        - location3
        - location4
        - communication
        properties:
          currentStatus:
            type: string
          currentSituation:
            type: string
          challengeType:
            type: string
          challenge:
            type: string
          degreeOfChallenge:
            type: string
          goalValueAdd:
            type: string
          desiredFutureSituation:
            type: string
          targetStart:
            type: string
          expectedEnd:
            type: string
          mainLocation:
            type: string
          additionalLocations:
            type: string
          location2:
            type: string
          location3:
            type: string
          location4:
            type: string
          communication:
            type: string
    responses:
      200:
        description: "successful attempt, returns `success message`"