/project/get-business-function:
  get:
    security:
    - Bearer: []
    summary: api for get business functions details.
    tags:
    - projects 
    description: api for get business function details.
    parameters:
    - name: X-Requested-With
      description: set value to XMLHttpRequest if you are making an xhr request
      in: header
      required: true
      default: XMLHttpRequest
    responses:
      200:
        description: "successful attempt, returns `success message`"