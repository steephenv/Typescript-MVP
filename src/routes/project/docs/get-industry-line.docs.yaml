/project/get-industry-line:
  get:
    security:
    - Bearer: []
    summary: api for get industry line details.
    tags:
    - projects 
    description: api for get industry line details.
    parameters:
    - name: X-Requested-With
      description: set value to XMLHttpRequest if you are making an xhr request
      in: header
      required: true
      default: XMLHttpRequest 
    responses:
      200:
        description: "successful attempt, returns `success message`"